"use client";
import { useEffect,useMemo,useState } from "react"; import { useParams,useRouter } from "next/navigation"; import { DEPRECIATION_GUIDE,GUIDE_CATEGORIES,guideRuleText } from "@/lib/depreciationGuide";
export default function EditItem(){const{id}=useParams<{id:string}>();const router=useRouter();const[item,setItem]=useState<any>(null);const[error,setError]=useState("");const[category,setCategory]=useState("");const[subcategory,setSubcategory]=useState("");const[manual,setManual]=useState(false);useEffect(()=>{fetch("/api/items/"+id).then(async r=>{if(!r.ok)throw new Error("Could not load item.");return r.json()}).then(x=>{setItem(x);setCategory(x.category||"");setSubcategory(x.subcategory||"");setManual(x.depreciationModel!=="guide"&&x.currentValue!=null)}).catch(e=>setError(e.message))},[id]);const options=useMemo(()=>DEPRECIATION_GUIDE.filter(x=>x.category===category),[category]);const rule=useMemo(()=>DEPRECIATION_GUIDE.find(x=>x.category===category&&x.subcategory===subcategory),[category,subcategory]);if(error)return <main className="container"><p className="negative">{error}</p><a className="button" href="/">Back to dashboard</a></main>;if(!item)return <main className="container"><p>Loading...</p></main>;
async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=Object.fromEntries(new FormData(e.currentTarget).entries());if(!manual)f.currentValue="";const r=await fetch("/api/items/"+id,{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});if(!r.ok){setError(await r.text());return}router.push("/items/"+id)}async function remove(){if(!confirm("Delete this item?"))return;const r=await fetch("/api/items/"+id,{method:"DELETE"});if(r.ok)router.push("/");else setError("Could not delete item.")}
const val=(k:string)=>item[k]??"";return <><header><div className="container nav"><a className="brand" href="/">Item Value Tracker</a></div></header><main className="container"><p><a className="muted" href={"/items/"+id}>← Back to item</a></p><h1>Edit item</h1><form className="card form" onSubmit={save}><div className="fields"><div className="field full"><label>Name</label><input name="name" defaultValue={val("name")} required/></div>
<div className="field"><label>Category</label><select name="category" value={category} onChange={e=>{setCategory(e.target.value);setSubcategory("")}} required>{GUIDE_CATEGORIES.map(x=><option key={x}>{x}</option>)}</select></div><div className="field"><label>Subcategory</label><select name="subcategory" value={subcategory} onChange={e=>setSubcategory(e.target.value)} required><option value="" disabled>Select a subcategory</option>{options.map(x=><option key={x.subcategory}>{x.subcategory}</option>)}</select></div>
{rule&&<div className="field full"><div className="card" style={{padding:12,margin:0}}><strong>Guide depreciation:</strong> {guideRuleText(rule)}{rule.notes&&<div className="muted" style={{marginTop:4}}>{rule.notes}</div>}</div></div>}
<div className="field"><label>Purchase date</label><input name="purchaseDate" type="date" defaultValue={String(val("purchaseDate")).slice(0,10)} required/></div><div className="field"><label>Purchase price (£)</label><input name="purchasePrice" type="number" step=".01" defaultValue={val("purchasePrice")} required/></div><div className="field"><label>Extra costs (£)</label><input name="additionalCosts" type="number" step=".01" defaultValue={val("additionalCosts")}/></div><div className="field"><label>Replacement cost (£)</label><input name="replacementCost" type="number" step=".01" defaultValue={val("replacementCost")}/></div>
<div className="field full"><label><input type="checkbox" checked={manual} onChange={e=>setManual(e.target.checked)} style={{width:"auto",marginRight:8}}/>Override guide value with my own current value</label></div>{manual&&<div className="field full"><label>Current value (£)</label><input name="currentValue" type="number" step=".01" defaultValue={val("currentValue")} required/></div>}
<div className="field"><label>Target sale price (£)</label><input name="targetSalePrice" type="number" step=".01" defaultValue={val("targetSalePrice")}/></div><div className="field"><label>Status</label><select name="status" defaultValue={val("status")}><option value="owned">Owned</option><option value="for-sale">For sale</option><option value="sold">Sold</option><option value="disposed">Disposed</option></select></div><div className="field"><label>Sale platform</label><input name="salePlatform" defaultValue={val("salePlatform")} placeholder="eBay / Facebook / Gumtree"/></div><div className="field"><label>Sale fees (£)</label><input name="saleFees" type="number" step=".01" defaultValue={val("saleFees")}/></div><div className="field"><label>Shipping (£)</label><input name="shippingCost" type="number" step=".01" defaultValue={val("shippingCost")}/></div><div className="field"><label>Packaging (£)</label><input name="packagingCost" type="number" step=".01" defaultValue={val("packagingCost")}/></div><div className="field"><label>Hours used</label><input name="hoursUsed" type="number" step=".1" defaultValue={val("hoursUsed")}/></div><div className="field"><label>Serial / model</label><input name="serialModel" defaultValue={val("serialModel")}/></div><div className="field"><label>Where bought</label><input name="source" defaultValue={val("source")}/></div><div className="field"><label>Condition bought</label><input name="conditionBought" defaultValue={val("conditionBought")}/></div><div className="field"><label>Current condition</label><input name="conditionCurrent" defaultValue={val("conditionCurrent")}/></div><div className="field full"><label>Photo URL</label><input name="photoUrl" defaultValue={val("photoUrl")}/></div><div className="field full"><label>Receipt URL</label><input name="receiptUrl" defaultValue={val("receiptUrl")}/></div><div className="field full"><label>Notes</label><textarea name="notes" defaultValue={val("notes")}/></div></div>{error&&<p className="negative">{error}</p>}<div className="actions"><button className="button primary">Save changes</button><a className="button" href={"/items/"+id}>Cancel</a><button type="button" className="button" onClick={remove}>Delete item</button></div></form></main></>}
